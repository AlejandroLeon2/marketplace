---
const { data} = Astro.props;
---

<button
  data-id={data.id}
  data-titulo={data.titulo}
  data-precio={data.precio}
  data-imagen={data.imagen}
  data-idprice={data.idprice}
  type="button"
  class="boton agregar-carrito text-sm flex px-4 justify-center py-2">{`S/. ${data.precio.toFixed(2)}`}</button
>
